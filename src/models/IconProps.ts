/**
 * Props for SVG icon components.
 */
export interface IconProps {
  /**
   * Optional class name to apply to the SVG element. This allows for customizing the styling of the icon.
   */
  className?: string;
}
